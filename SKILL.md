---
name: guard-ratchet
description: 在任意仓库落地「只许变好」的源码守卫（基线棘轮）：圈复杂度、文件/函数体量、测试形态（无断言、.only、长 sleep、永久 skip）、死代码（无引用文件/导出）、重复代码、运行形态（循环里逐条查库、深层嵌套循环、无条件 SELECT）、循环依赖。现有问题记进基线不改业务代码，新增违规、变差、基线虚挂都让 lint/CI 失败。只要用户想给仓库加代码质量门禁、防止复杂度/重复/死代码回潮、「新代码不许更差、旧债只许还」、给 lint 链加检查脚本、设计 baseline / ratchet / code health gate / quality gate / fitness function，或者想把某类代码问题变成自动检查——即使没说「守卫」或「基线」——都应使用本 skill。不用于：一次性清理或重构现有代码、跑测试、修单个 lint 报错、只想要一份代码审查意见。
---

# 基线棘轮守卫

目标：让仓库在代码形态上**只能变好**。现有问题不要求当天修完，记进基线；从此以后，新增的问题、已有问题变得更严重、以及已经修好却没从基线删掉的条目，都会让检查失败。

这套做法之所以有效，是因为代码变坏几乎从来不是一次改动造成的——一个函数从复杂度 20 涨到 50 是很多次「只加一点」累积的。棘轮把每一次「只加一点」都变成显式决定：要么修掉，要么在提交里写明理由更新基线。

## 先分清两层

| 层 | 内容 | 是否随仓库变化 |
|---|---|---|
| 棘轮 | 发现与基线怎么比、什么算失败、基线怎么更新 | 不变。规格见 `references/contract.md`，参考实现 `scripts/ratchet.py`，一致性用例 `conformance/cases.json` |
| 检测器 | 在这个仓库里找出「发现」 | 随语言、架构、约定而变。由你在当前仓库挑选或编写 |

检测器只负责输出发现（规则、稳定的位置标识、计数）；比对逻辑必须符合契约。检测器可以是仓库已有工具的输出转换、成熟的现成工具，或者自己写的 AST 脚本。棘轮可以直接调用 `scripts/ratchet.py`，也可以在仓库的主语言里实现——只要跑通 `conformance/cases.json` 里的全部用例。

## 工作流程

按顺序做。每一步的产出都要能对用户说清楚。

### 1. 摸清仓库

在动手之前了解：
- 语言、目录结构、哪些是正式代码、哪些是测试、哪些是生成物或第三方代码
- 入口在哪里，以及**约定式加载**：按目录扫描加载的插件/hook、框架按文件名加载的配置、测试里用字符串路径加载模块的辅助函数、路由按文件系统注册等。死代码和循环依赖检测不认识这些就会大量误报
- 已有的 lint、类型检查、CI 流水线、pre-commit；已有的守卫脚本和它们的写法
- 是否有其他人或其他 agent 会话正在同一分支上改代码（影响第 5 步的基线生成）
- 当前 lint/CI 是否本来就是红的——如果是，先告诉用户，不要让新守卫背锅

### 2. 选守卫

读 `references/guard-catalog.md`，按仓库实际情况挑，不要全上。例如没有数据库就没有「循环里查库」；Go 编译器本身禁止包循环依赖，就不需要循环依赖守卫。用户点名了的守卫照做；用户只说了大方向时，先提出建议清单和理由，得到确认再做。

对每条规则决定它是**硬规则**还是**基线规则**：
- 硬规则：违反即失败，不进基线。适合「现在是 0 处、而且一旦出现就几乎肯定是错误」的情况，例如提交了 `.only`、测试没有断言、出现循环依赖。
- 基线规则：现有违规记进基线，只许减少。适合存量多、一时修不完的问题。

门槛（复杂度上限、重复片段最小 token 数、延迟毫秒数等）用户给了就照用，不要为了让现状通过去调；用户没给就用目录里的默认值并说明。

### 3. 选检测器

按这个顺序，前一种不够用才用下一种：
1. 仓库里已有的工具或脚本（例如 ESLint 的 complexity 规则、已有的依赖图工具）
2. 成熟的现成工具，把它的输出转换成发现（见 `references/language-recipes.md`）。注意固定版本——工具升级改变计数会导致基线大面积变动
3. 用该语言的解析器自己写（AST，不要用正则匹配代码结构）

引入新依赖前先问用户，除非用户已经允许。

### 4. 实现

每个守卫都要满足 `references/contract.md` 里的命令行约定：`--update-baseline` 重写基线、默认只检查、退出码 0/1/2，以及用于测试的 `--root` 之类参数。尽量让每个守卫在几秒内跑完——它会进入每次 lint。

位置标识（key）的设计是基线稳定的关键：用「路径#符号名」或规范化后的内容指纹，**不要用行号**，否则任何无关的插行都会让整份基线失效。同名冲突用 `~2`、`~3` 这类稳定后缀区分。

### 5. 生成基线

- 用当前代码生成基线；**不要为了让守卫通过去改业务代码**。现有违规记录下来就是基线存在的意义。
- 如果工作区里有别人未提交的改动，从干净的提交快照生成基线（例如 `git archive HEAD` 解到临时目录，再拷入自己本次改动的文件），避免把别人的半成品记进基线。
- 硬规则现在就有违规时，停下来和用户商量：修掉这些违规（通常是测试或配置，而不是业务代码），或者把它降级为基线规则。

### 6. 接入

挂进仓库原有的 lint 或 CI 链，放在同类检查旁边。给每个守卫一个一致的命令名（例如 `check:duplication`），在脚本头部注释里写清：查什么、为什么、怎么更新基线。

### 7. 验证

每个守卫都要有夹具测试：在临时目录搭一个小仓库，覆盖三种结果：
1. 现状与基线一致 → 通过
2. 新增一条违规 → 失败，并且报出的是这一条
3. 修好一条基线里的违规但没更新基线 → 失败（虚挂）

硬规则只需要前两种。把这些测试也接进 lint，否则守卫坏了没人知道。然后在真实仓库上直接运行每个守卫，确认退出 0；如果棘轮是自己实现的，跑一遍 `conformance/cases.json`。

### 8. 汇报

向用户说明：
- 加了哪些守卫、各自的命令、哪些是硬规则
- 每个基线里有多少条，挑几条有代表性的说明它们是真问题还是误报
- 已知误报来源和打算怎么处理
- 为了让硬规则当天通过改了哪些非业务文件
- lint/CI 是否整体通过；没通过的话，是哪一步、是不是本次引起的
- 以后别人修好问题时要记得更新基线（否则会被报虚挂）

## 常见坑

落地前读一遍 `references/pitfalls.md`，里面是真实落地时踩过的坑：约定式加载导致的误报、被 git 忽略但存在于本地的目录、并发会话污染基线、测试 glob 实际上没排除掉目录等。

## 参考文件

- `references/contract.md` — 棘轮语义、发现与基线格式、命令行约定。实现或移植棘轮前必读
- `references/guard-catalog.md` — 每种守卫的目的、信号、key 设计、默认门槛、常见误报
- `references/language-recipes.md` — 各语言可用的解析器和现成工具
- `references/pitfalls.md` — 落地时的坑
- `scripts/ratchet.py` — 棘轮参考实现（Python 标准库，无依赖）；`python3 scripts/ratchet.py conformance` 运行一致性用例
- `examples/javascript/` — 一套落地过的 JavaScript 实现（espree 解析、6 个守卫、夹具测试），写自研检测器时参照；其中的扫描目录和约定属于原仓库，先读它的 README
